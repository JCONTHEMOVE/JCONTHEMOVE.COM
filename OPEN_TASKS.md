# JC ON THE MOVE completion register

Updated September 9, 2026. This register distinguishes checked code from live service completion. No item is closed merely because its prior Codex turn ended.

## Current release and service

- My Jobs synthetic browser recovery passed: an approved unpaid job exposes Retry final invoice, disables it while pending, restores it after an intercepted 503, and refreshes the open details to a payment link after the second successful request. The completed-job banner incorrectly promised tokens shortly despite the unpaid balance; it now explains that eligible rewards require verified payment and completion. This uses intercepted responses and an example.invalid destination, not an authenticated live job. The desktop details had no horizontal overflow; phone-width details and owner/provider acceptance remain open.

- Synthetic browser acceptance for the closeout-link recovery page: at 320px the saved approval shows a 44px Retry final invoice action, disables it while pending, returns it after an intercepted 503 and reaches the payment-link result after a successful intercepted retry. The result originally overflowed by 19px with a 40px payment link; the card now permits shrinking and the link wraps, producing 320px content/scroll width and a 64px link. This uses fake responses and an example.invalid payment destination. My Jobs browser acceptance, real owner/device and provider-flow acceptance remain open; no real payment request or message was sent.

- Customer invoice retry controls are implemented on the closeout-link page and My Jobs. A server presentation hint exposes the action only for a recorded approved closeout while canonical payments are enabled; the transactional endpoint still revalidates funding and quote. Both pages refresh state after an invoice error, retain pending/disabled behavior and label the action Retry final invoice. The availability test passes. Rendered browser/device and full endpoint/provider acceptance remain open; no customer action was submitted.

- Canonical invoice errors now preserve customer approval instead of clearing it after an ambiguous provider response. The authorized closeout endpoint can resume an approved request; the transactional approval service rechecks current funding and quote, requires the original approval evidence/due date and reuses the original request identity without rewriting approval. Disposable SQL tests cover unchanged retry, retained timestamp and rejection of missing approval or changed payments. Full endpoint/provider failure recovery and automatic replacement issuance still require acceptance.

- Partial-payment recovery correction: canceled/noncollectible final invoices now leave the verified remainder on the lead and closeout, remove the obsolete payment link and keep reconciliation pending. The old invoice binding is retained for audit. Disposable worker tests cover the $120 job/$60 paid/$90 canceled invoice case, transaction rollback and late attachment. Creating the corrected replacement invoice remains open; no live invoice or notice was produced.

- Current application evidence: `0ac63b3dce7d95db7328bc63d4be3cd5c8744f72` passed every Release Candidate Validation step in [run 34424405420](https://github.com/JCONTHEMOVE/JCONTHEMOVE.COM/actions/runs/34424405420). Earlier commit/run references below are historical evidence, not the current candidate. Production remains at the last inspected `25b985e4`; passing candidate CI does not establish deployment or owner acceptance.
- Open-issue audit: GitHub still lists #4 (mobile lead details), #7 (payment/reward pipeline), and #3/#6 (Pi Jackpot). No issue was closed. The moving-service candidate implements much of #4/#7, but authenticated workflow acceptance, provider acceptance and release remain outstanding. Pi Jackpot remains tracked separately. This audit covers repository issues; it does not establish that every older Codex request has been retrieved.
- Current payment gaps: signed-provider-to-wallet acceptance; corrected replacement invoices after partial funding; duplicate-invoice review; concurrent quote/refund/completion/assignment acceptance; tipped-refund allocation and issued-reward policy; production schema/backfill and isolated recovery; specifically authorized live acceptance. Immutable provider intent, saved customer/order/invoice identities, exact-funded closeout finalization, financial notice delivery and audited notice review are now implemented in the candidate. Older entries calling those implementations open are superseded by this status; their provider and owner acceptance is still open.

- Canonical final invoices now record immutable financial/recipient input before the first provider call. A failed or unacknowledged intent write stops provider entry, and changed retry inputs require reconciliation. The actual-service/disposable-SQL boundary test covers amount, recipient, description, due date, delivery mode and location changes. The new intent table is part of the unreleased ledger schema. Customer/order/invoice phase identities and the initial invoice version now persist and are reused on retry; unknown order outcomes replay the same provider request. Competing phase identities reject. Full signed-provider acceptance and recovery beyond provider idempotency retention remain open; no live invoice was created.
- Replit backup recheck at approximately September 10 00:51 UTC: the original production database remains Active with seven-day PITR and seven-day daily backup retention; its backup list still says No backups yet. Current Replit documentation places scheduled backups near midnight in the browser time zone used when configured, so this pre-midnight observation does not establish a failure. Recheck after the first scheduled time. The documented Restore action switches the connected database rather than proving an isolated target; successful backup and isolated recovery remain open. No settings, credentials, database contents or additional databases changed. See DATABASE_RECOVERY_RELEASE.md for sources and the drill gates.
- Canonical financial notices persist with invoice attachment and paid closeout, including zero-balance approval. The draft worker now rechecks financial state and channel consent, records portal events, uses direct Gmail/SMS adapters and retains uncertain send attempts for review. Disposable tests cover stale notices, payment/consent changes between channels, unavailable providers, lost commit/acknowledgement responses and legacy history. See JOB_PAYMENT_LEDGER_RELEASE.md for the delivery contract and limitations. Audited review controls are now implemented; actual provider delivery and controlled owner/customer acceptance remain open; keep canonical flags off. No email or SMS was sent.
- Financial closeout now commits with reconciliation completion: exact verified funding, customer-approved final quote, completed job, unchanged invoice set and current queue claim are rechecked under locks before marking paid and clearing the stale payment URL. A late invoice-attachment request returns the existing paid outcome instead of restoring balance due. Disposable tests cover partial-write rollback/retry, attachment rollback, and payments/new invoices appearing before finalization. Customer lifecycle message delivery, unpaid replacement invoices, review handling and live acceptance remain open; no production flags, payments or messages changed.
- Invoice collection worker implemented in the candidate: it checks current quote/closeout/payment bindings, inspects the Square invoice and order at the configured location, rechecks its claim after HTTP, and cancels excess collection without treating that invoice's own payments as new excess. A periodic scan recovers missing requests and revisits local draft/sent invoices. Tests cover full/partial other-order payments, own-order payments, changes during inspection, unverified paid/refund states, backlog scans and the actual SDK with intercepted HTTP. Startup requires all three ledger/adapter/reconciliation flags; none were enabled. Production/provider acceptance, pre-provider intent, final closeout/customer messaging, duplicate-invoice resolution and owner-review handling remain open. No live invoice was canceled.
- Durable invoice reconciliation handoff: new canonical payments/refunds and final-invoice publication acknowledgements now save a per-job reconciliation request in the same database transaction. Queue generations preserve changes arriving during an active claim; lease tokens fence expired claims. Local disposable checks prove rollback on failed queue writes, duplicate-event suppression, newer-work retention, lease replacement and retry delay. The additive queue schema is included in the unreleased ledger schema; no production migration ran. Claiming is gated by `JOB_INVOICE_RECONCILIATION_ENABLED`, default off. Live provider/backlog acceptance, pre-provider invoice intent and end-to-end cancellation/closeout acceptance remain open.
- Cancellation recovery: the existing invoice-cancel service now checks provider identity, eligible state and current version; an ambiguous response triggers a fresh provider read. Already-canceled invoices retry local acknowledgement without another cancellation request. The atomic local write rejects paid/refunded or unexpected status instead of overwriting it, and retains payment timestamps. Simulated service tests cover lost provider/local responses, payment/version races and invalid states; the actual SQL checks are included in PostgreSQL CI. The disabled reconciliation worker uses this recovery operation. No live invoice was canceled.
- Invoice recovery preparation: lead invoices now persist their provider ID, order/quote/closeout bindings and amount as a draft before publication. Local insert failure prevents publication; lost publication responses retain the recovery row. A single SQL acknowledgement fills publication metadata and advances only draft status, preserving paid/closed webhook state and timestamps on retry. Local service tests with a simulated provider and actual PGlite SQL checks pass; the SQL check is also registered in PostgreSQL CI. The prepublication guard and disabled reconciliation worker build on this recovery record. Production is unchanged.
- Late-payment regression: the candidate now rechecks canonical final invoices immediately before publication against the current closeout approval, quote, total, payments and refunds under the job lock. The former $120/$90 defect reproduction now verifies that a payment committed during invoice creation prevents publication and leaves a recovery draft; it runs in normal PostgreSQL CI. Local PGlite tests pass for this interleaving, partial late payments, refunds and changed approval/bindings. The lock ends before provider HTTP, so payments after the check or after publication still require durable intent and cancellation/reconciliation recovery. Those release gates remain open; no live provider request occurred.
- Live marketing inspection: Northwoods Gmail OAuth is pending; polling, scanning and scheduling are disabled, with no processed message or confirmed opening. Matt Page OAuth is pending. All three legacy company Facebook Pages show reauth required; primary Page 912756211920086 was last verified August 26. Existing Verify does not renew authorization, so secure reconnection remains required. No campaign, availability or connection was changed. The dashboard incorrectly counted three ad-enabled markets as verified even though two were pending; the candidate now counts only ad-enabled markets with verified status (the current visible records imply one eligible market).
- Square readiness correction in the draft: the launch probe now always requests provider locations, including when a location ID is configured, instead of allowing a cached/configured ID to bypass authentication. It validates the selected location is active, honors environment-specific credentials, rejects sandbox on a production host, bounds the request and redacts provider error bodies. This does not repair the live credentials or prove invoice/payment delivery; owner Square sign-in remains pending.
- September 9 read-only Launch Checklist audit completed across three inspections: 27 of 29 distinct probes ran, with 26 green and Square authentication failed. The final 16 passed pricing/redemption/classifier checks, wallet and fund configuration, public URL/bundle checks, marketplace model/wiring, promo integrity (22 of 171 active), and rep-page integrity (5 active pages). BTC checks pass because BTC is optional/disabled, not because payments work. Public-route inspection explicitly skipped the source audit and only found a production client bundle; reward/marketplace checks inspect aggregate records and source/bundle markers, not actual transactions. The two unrun probes are deposit-gate synthetic lead creation/deletion and tracked marketing synthetic record creation/deletion. No test records, payouts, rewards or outbound notifications were produced by this audit. These results do not close live payment, notification, recovery or owner-workflow gates.
- Additional September 9 authenticated read-only probes passed: Gmail notification configuration, quick-request storage columns, launch payout defaults, payout-history scan, core booking catalog (21 active services), and approved admin/owner account existence (4 accounts). No delivery, payout or booking was performed. The payout scan checks lead statuses and found no matching premature records; it does not prove approval enforcement during a real payout. Across this and the preceding inspection, 11 of 29 distinct probes were executed: 10 passed and Square authentication failed. Eighteen remain unrun, including the two probes that write/delete test records. Square still requires owner sign-in.
- September 9 authenticated production probes: environment presence, database readiness, current deployed build and custom-domain checks passed. Live commit remains `25b985e4`; the freshness probe compares that deployment with itself, not this release draft. Square configuration failed authentication with `environment=sandbox`. Square developer access requires sign-in; the Chrome login page was left open for the owner. No token, environment or payment was changed. Only five individually reviewed read-only probes ran; Run all was avoided because it also creates/deletes synthetic lead and marketing records. The other 24 probes remain unrun in this session.
- Authenticated live workflow access is available through Chrome (the in-app site session is signed out). Read-only inspection of the previously controlled JC-87 job reached its quote-ready lead page with one Send Quote & Invoice action and non-JSON notes. Live Send/Save controls measured 40px; candidate Save/Reset now join its primary action at 44px minimum. Chrome's attempted 390px override reported a 747px document, so that check is not phone-width acceptance. No quote, payment, job change or crew notification was submitted.
- GitHub #4 package summary: lead details now show the selected package name, stored price/range, duration, explicit flat-rate/hourly label, deposit requirement and description together. Unknown terms say To be confirmed. Synthetic full-page checks keep customer notes separate and show no overflow at 320px dark and 1280px desktop widths. Live authenticated workflow acceptance remains open.
- Quote draft race fixed in the candidate: calculation rechecks the same job input fields under its write lock and rejects stale saves. Disposable tests verify edits committed during calculation cannot insert or overwrite a draft; fresh retries succeed. Concurrent payment effects and post-settlement policy remain separate open gates.
- Replit recovery follow-up: settings still confirm seven-day PITR and daily backups; no successful backup or isolated restore was proven. The existing PR #8 read-only SQL collector is now included in this draft with a disposable execution test and `DATABASE_RECOVERY_RELEASE.md`. The production table overview includes legacy job payment records but does not list the draft canonical ledger tables; it is not schema/migration acceptance.
- Invoice retry follow-up: the actual lead-invoice service passed isolated Node 20 tests for four lost provider responses, failed local inserts, committed-but-unacknowledged paid records and changed retry payload conflicts. The provider is simulated and all database/network access is blocked. Live Square acceptance and mutable-request recovery remain open.
- Latest completed application validation: full CI passed at `7c69979341c0285678f3f313d074964d4f531a63`, run 34422269027, including mobile package/tap-target fixes, stale quote rejection, restore collector execution, invoice retry orchestration, payment/refund concurrency, reward rollback, types, server tests, monitoring, build and PWA checks. See `JOB_PAYMENT_LEDGER_RELEASE.md` for scope and remaining gates. Railway production was not deployed. GitHub reports an automatic Vercel branch preview; it is not production or acceptance evidence.
- Replit isolated-preview investigation: inspected Publishing overview, Adjust settings and Manage on the original project. No isolated-preview action was exposed in those panels. The available development-to-production database copy explicitly overwrites existing production data and was left unchecked. A separate recovery destination is still needed; documentation describing previews alone does not establish one for this project.
- September 9 Replit recovery update: enabled automatic daily backups with seven-day retention on the existing Production Database. The UI confirms `Scheduled backups On · kept 7 days`; PITR remains `On · last 7 days`. It still reports `No backups yet`, so first-backup success and isolated restore are not verified. No live restore, credential rotation or database migration occurred. This supersedes the earlier scheduled-backups-off observations below.
- User priority: Replit first, then potentially up to two Neon databases. Start with the existing Replit project and its production database; additional databases are optional, not a requirement to create or migrate them. The supplied production hostname already matches Replit's production database.
- Public readiness on `www.jconthemove.com` reports Railway, a connected database, and commit `25b985e4`.
- Domain restoration passed: Cloudflare rule `f14b13f5da1e4b4582616580b2913a0e` redirects apex requests to HTTPS www with status 308, preserving path, query and method. HTTP/HTTPS home and booking probes passed, including Matt referral rendering.
- The candidate contains the pre-existing Quick Book, phone entry and push readiness changes plus completion fixes. The application candidate has not been deployed; the Cloudflare domain repair is live.
- User approved publication September 9. Candidate `ae9020cc` is pushed on `agent/open-task-completion-20260908`; draft PR #10 is open. Release CI run 34367726253 passed all steps, including Node 20 types. No merge or application deployment occurred.

## Delivery queue

| Work | Evidence and remaining completion condition |
| --- | --- |
| Domain restoration | Completed September 9: active apex-to-www 308 rule; all five entrypoint checks and four explicit HTTP/HTTPS redirect checks passed. Browser booking page retained the Matt referral. |
| Phone entry | Shared formatting, digit count and server validation are implemented. This pass fixed uppercase/trailing-slash guard bypasses and accessible labels. Browser checks passed for missing/extra digits, formatted paste, controlled save, form reset, legacy edits and 390px layout. Release still required. |
| Mobile lead details / GitHub #4 | This pass separates customer notes from package JSON, preserves package metadata on edits/clearing, and enlarges secondary tabs. Focused preservation test passes. Remaining full owner-page workflow acceptance must be verified before closing #4. |
| Quick Book | Existing local code uses disabled-by-default owner flags. 49-file server validation includes quote/permission checks; disposable migration SQL passed repeated execution and retention checks. Clean install, production build and exact-commit CI type checking passed; owner fixtures and exact live release remain required. See `QUICK_BOOK_RELEASE.md`. |
| Historical Replit booking review math task | September 9 live browser reproduction: Lake Tomahawk (~66 miles), one two-seat couch, load only, customer truck, September 15 flexible time. Inventory showed $150–$225; review showed $560 with consistent math (2 movers × 2 hours × $140) and outside-zone owner review. No booking was submitted. The earlier inventory range ignores service-zone pricing; changed its preview to recommended crew/truck and directs customers to the location-aware review estimate. Pricing parity and booking experience alignment tests passed. This follow-up extends candidate ae9020cc on the same release branch; fresh CI, release and live verification remain required. The original $270/$425 report was not reproduced exactly. Replit's automatically Suggested tasks are suggestions, not accepted completion requirements. |
| Web push | Existing readiness and single-use owner test code is present and included in passing server suite. Neither the workspace nor the filtered Railway production variable inventory has a VAPID pair. Configure the intended production pair and verify one authorized owner-device receipt before relying on crew push. |
| PWA installation assets | Live `/manifest.json`, `/icons/icon-192x192.png` and `/offline.html` incorrectly return the 12,195-byte HTML app shell. Copied the 21 existing manifest/offline/favicon/icon assets into `client/public`, Vite's configured default public directory. Fresh Node 20 build passed; the new build verifier confirms the manifest, all 16 declared PNG dimensions, favicon, offline page and push service worker. The verifier rejects the old build's missing manifest and is included in release CI. Deployment and live HTTP acceptance remain open. The assets and build verifier are included in the current 90-file candidate. The copied favicon was SVG mislabeled as ICO; it is now a valid ICO containing the existing 72px PNG. |
| Availability alerts / PR #8 | PR #10 now combines PR #8 incident-service reporting and isolated drills with apex/www/referral checks; the duplicate direct Discord reporter was removed. Twelve fake-HTTP tests pass and the workflow parses. Darrell is owner and technical responder; email is upmichiganstatemovers@gmail.com. Exact Discord destination, monitoring account/plan and cost, private heartbeat secrets, independent missed-check coverage, actual receipt/escalation/recovery and delayed-run/retry acceptance remain open. Follow AVAILABILITY_RELEASE.md; no live alert or subscription was created. |
| Database recovery / PR #8 | Production ownership confirmed September 9: Replit's original JC ON THE MOVE app shows standalone PGHOST `ep-divine-band-aft2830l.c-2.us-west-2.aws.neon.tech`, matching the hostname supplied from Railway. Production is active, with seven-day point-in-time recovery enabled. Scheduled daily backups are enabled with seven-day retention. The latest September 9 recheck still reports No backups yet; first-backup success remains unverified. The restore dialog exposes a timestamp but no separate destination, so it was canceled without restoring live data. An isolated recovery test remains open. The two separately connected us-east-1 Neon projects do not match production and are not production recovery evidence. |
| Canonical payment/reward pipeline / GitHub #7 | The draft now connects verified Square payment/refund adapters to the signed webhook behind disabled flags. Canonical invoice classification uses a consistent payment snapshot and closeout approval; customer approval records the final quote and job total atomically. Invoice retries retain stable phase keys and due dates. Payment/refund ledgers, durable reward queue, fenced claims, wallet settlement, cash credit and revenue allocation have disposable database and PostgreSQL concurrency coverage. Full CI passed at 5bd24a4d. Still open: complete provider/signature-to-effects acceptance, late-payment/final-invoice coordination, tipped refunds, owner policy for issued rewards, production schema/backfill/recovery, owner UI acceptance and specifically authorized controlled live payment/reward verification. Automatic flags remain off. See JOB_PAYMENT_LEDGER_RELEASE.md. |
| Square gift-card pilot | Earlier work remains dependent on intended Square environment and a private purchase/reward reconciliation test. No purchase or reward issuance was performed here. |
| Northwoods imports | Existing code/tests are present; intended mailbox OAuth and controlled import-through-review remain open. |
| Ashley photo/email | Existing code/tests are present; separate intended Gmail connection and multi-image draft acceptance remain open. Ashley retains final approval. |
| Facebook marketing pilot | Page-locked pilot has prior implementation; current Meta role/OAuth acceptance and an explicitly scoped live campaign test remain open. |
| Single-Page reconnect and weekly campaigns / PR #9 | Reviewed all six marketing source/test changes. Added an isolated Node 20 release workflow on the existing PR branch. Head 4a5eda9a passed clean npm ci, full types, the complete discovered server suite and client/server build in run 34401078652. It restores one-Page reconnect for JC On The MOVE : CoM and adds Minocqua Monday, Iron River Tuesday, Eagle River Wednesday and crew-appreciation quote-request formats. This separate branch is not part of PR #10. Remaining: renew the selected Page authorization, verify the import gate and real restored connection, complete the separate invitations/OAuth for Matt, and obtain the specific release/campaign approvals. No campaign publication is authorized by this register. |
| Legacy deployment PR #1 | Reviewed: GitHub reports merge conflicts; its Render blueprint differs from current Railway production. Current source already ignores `.env` and does not track it. Do not merge the old hosting configuration wholesale. The historical diff exposes a treasury private key: confirm that wallet was retired/replaced and no active service relies on that key. Deleting `.env` from a later commit does not erase history. No wallet operation was performed here. |
| Pi Jackpot GitHub #3/#6 | Separate product work is tracked by `JC's Pi Jackpot — Master Completion Task`. Do not mix its payment credentials, simulator or database into the moving-service release. These issues remain open pending that product's evidence. |

## Verification recorded in this pass

- Availability consolidation: PR #10 now incorporates PR #8's incident-service reporter and isolated drill workflow, retains the apex/www/referral checks, and removes the separate direct Discord reporter. Twelve reporter/entrypoint tests pass with fake HTTP; the combined workflow parses and all production HTTP steps are excluded from drills. Activation, actual recipient delivery, independent missed-check coverage and delayed-run/retry behavior remain open. Follow AVAILABILITY_RELEASE.md; do not merge an older availability workflow over this combined version.
- Long-content mobile check: the full synthetic lead page initially expanded to 709px inside a 320px frame. Customer names, crew names/roles and the address link now wrap within their containers. Browser measurements after the fix found no horizontal overflow at 320/390/768/1280px frame widths (305/375/753/1265px content widths after scrollbar space). Crew rows retain at least 44px height. This uses local synthetic data with writes disabled, not live owner-account or device acceptance. Dispatch-readiness application commit 67590774 passed full CI run 34399301016 before this layout-only follow-up.
- Mobile lead follow-up: dispatch now explains missing saved price, valid service date and distinct named crew, and stays disabled until those fields are ready. The manual mark-paid route checks the same requirements before dispatch payment writes (the separate completed-job path is unchanged). Readiness tests cover zero/invalid prices, impossible dates and duplicate/incomplete crew; they do not prove the full route or owner workflow. Header actions now wrap and crew-selection rows have 44px minimum height. Authenticated phone/desktop acceptance remains open.
- Existing Node 20 worktree: all 48 server test files passed before the notes change; the added notes test passed separately.
- Phone route-alias regression and all 19 monitoring tests passed after their fixes.
- Disposable Quick Book/regional SQL applied three times; status/FK constraints, indexes, agreement columns and transcript retention passed. This does not test production schema drift.
- Type check passed on the installed Node 24 runtime. Local Node 20 checks were stopped under severe memory pressure (about 70 MB free on an 8 GB host); the subsequent exact-commit repository CI type check passed on September 9.
- Fresh clean-install Node 20 client/server production build passed after the notes/accessibility, synthetic fixture and PWA changes. All 688 tracked application source files matched the validation copy by hash. Vite reported large chunks and outdated browser metadata warnings. The isolated validation directory has no Git identity, so its build marker is intentionally missing and is not deployment evidence.
- New `Release Candidate Validation` workflow runs Node 20 clean install, types, server tests, monitoring tests and build on pull requests without production secrets or deployment. Run 34367726253 subsequently passed all steps for candidate ae9020cc.
- A new clean Node 20 `npm ci` succeeded under sibling `.verify`: 1,509 packages installed in 17 minutes. The old `.quick-book-validation` clean install had failed and is not accepted as proof.
- Clean-install server validation now covers all 49 test files: 34 passed in the original run, the stalled pricing file passed on retry, and the remaining 14 passed with individual 60-second limits. This was a resumed run, not a single uninterrupted suite.
- Local Express serving of the fresh build returned the real manifest (200 application/json, 3,027 bytes), 192px icon (200 image/png, 3,978 bytes), and offline page (200 text/html, 4,114 bytes). JSON identity, PNG dimensions and offline title were asserted. This exercised static serving only, without starting the database-backed app.
- A final clean-install TypeScript retry also stalled with roughly 300 MB free. Only its verified compiler process was stopped; it did not pass. Live deployment/acceptance remains unverified despite the passing build.

## Sources and access limits

Sources: current worktree, production HTTP/browser checks, recent Codex tasks, GitHub open issues #3/#4/#6/#7 and PRs #1/#8, and read-only Neon organization/project/branch listings. The task list exposes 50 recent unpinned tasks, so older historical requests may still need retrieval.

GitHub CLI is signed out, but Git transport works outside the Windows sandbox and the GitHub connector can manage pull requests. Cloudflare access is confirmed and the domain repaired. Railway access is confirmed for project grateful-embrace, service jc-on-the-move, production. Replit access is confirmed: its production database's standalone PGHOST matches the production hostname supplied by the user. Fresh organization, project and endpoint listings show no match in the separately connected Neon account. No production database mutation, payment, broadcast or production deployment was performed. Controlled live acceptance and isolated recovery verification remain outstanding work, not completion claims.

September 9 release evidence: https://github.com/JCONTHEMOVE/JCONTHEMOVE.COM/pull/10 ; all CI steps passed at ae9020cc: https://github.com/JCONTHEMOVE/JCONTHEMOVE.COM/actions/runs/34367726253 . Earlier local type-check failures are superseded by this exact-commit CI result.

September 9 Railway inspection: project d3bc9bdd-138d-4ee6-a6f4-0174785b4ad1, service 51ce6c5a-a468-458b-bc8c-9080f479a634, environment e12e6fc8-f46b-4f26-88df-1114e0dc6fcc. Current deployment is healthy on Node 20.20.2. Source is main with automatic deployments enabled; Wait for CI is false. Healthcheck Path is /health (process liveness); /api/health is the stricter readiness endpoint in source. VAPID and QUICK_BOOK variable filters returned no matches. No service variables, deployment settings, credentials or application deployments were changed during inspection.

Production hostname supplied by the user September 9: ep-divine-band-aft2830l.c-2.us-west-2.aws.neon.tech. Both connected organizations and all five endpoints in their two projects were checked; none matches. Do not use either unrelated project's history settings as production backup evidence. No credential was revealed or database changed.

September 9 Replit inspection: the original JC ON THE MOVE Production Database has the matching PGHOST, is Active, and uses approximately 103.74 MB of 100 GB. Point-in-time recovery shows On, last 7 days; scheduled backups show Off, with a 7-day retention option. The restore dialog showed only a timestamp and Restore action, with no isolated destination selector; it was canceled. This verifies configuration, not a successful recovery. The development database is separate (approximately 43.98 MB of 20 GB); Replit's previous-Neon-development-database removal notice applies to development, not this production database. No password or full connection string was revealed, and no backup settings were changed.
