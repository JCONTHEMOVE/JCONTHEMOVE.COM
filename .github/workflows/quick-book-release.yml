name: Quick Book release checks
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run check
      - name: Quick Book extraction and guards
        run: |
          node --import tsx server/services/__tests__/quickBook.test.ts > quick-book-test.log 2>&1
          cat quick-book-test.log
          grep -q 'Quick Book extraction, gate, and transaction guardrail tests passed' quick-book-test.log
          node --import tsx server/services/__tests__/quickBookRelease.test.ts
          node --import tsx server/services/__tests__/quickBookAvailability.test.ts
          node --import tsx server/services/__tests__/quickBookSchedule.test.ts
          node --import tsx server/services/__tests__/quickBookCommit.test.ts
      - run: npm run test:server
      - name: Phone rewards enrollment guards
        run: |
          node --import tsx server/routes/__tests__/phoneRewards.test.ts > phone-rewards-test.log 2>&1
          cat phone-rewards-test.log
          grep -q 'Phone rewards: consent, SMS failure, throttling, invalid code, duplicates, privileged profiles, replay and retry checks passed' phone-rewards-test.log
      - run: npm run build
