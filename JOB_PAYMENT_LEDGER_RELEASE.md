# Canonical payment ledger — implementation and release gates

The initial service is `server/services/jobPaymentLedger.ts`. It is **disabled by default and not wired to any route or startup migration**. `JOB_PAYMENT_LEDGER_ENABLED=true` alone does not switch Square or wallet checkout to this service. Do not enable it in production yet.

Implemented: immutable `(provider, provider_payment_id)` identity, integer-cent input validation, USD-only settlement, a per-job row lock, cumulative confirmed payments, separately stored gift-funded cents, atomic payment recording and paid timestamp, and rejection of conflicting replays. The service does not dispatch, issue rewards, send messages, or change existing provider handling.

Verification: the policy test covers invalid amounts/currency/timestamps, partial and full payment, one-cent shortfalls, overpayment, and gift-funded exclusions. `scripts/check-job-payment-ledger.ts` runs the real service against a disposable PGlite database. It passed repeated schema execution, the disabled gate, cumulative settlement, duplicate/conflicting replay, gift exclusion, transaction rollback, and successful retry without another payment row. This is not a concurrent-session or production-schema test.

Remaining requirements from issue #7:

The durable reward queue and worker are implemented: unique job handoff, atomic enqueue during completed/full-payment confirmation, a sweep for payment-before-completion, expiring worker claims with unique tokens, stale-worker rejection and bounded retry timing. The worker calls the existing disbursement path and verifies completion stamps, customer/assigned-crew ledger entries, funding, wallet markers and matching reward records before finishing a claim under the job lock. Pending customer claims remain explicitly reserved in the ledger. Startup schedules at most ten claims per minute only when `JOB_PAYMENT_LEDGER_ENABLED`, `JOB_PAYMENT_REWARDS_ENABLED`, and `JOB_PAYMENT_REWARD_WORKER_ENABLED` are all `true`. No production flag was enabled. Disposable tests passed deduplication, lease expiry, stale completion rejection, retry scheduling, incomplete-proof/missing-crew rejection, completed proof without duplicate credit, and payment-before-completion handoff. Server build passed. Full worker/provider acceptance and migration remain open.

Separate-session acceptance passed in full release CI at d2732cad, run 34386188040, with a disposable PostgreSQL 16 service and `scripts/check-payment-concurrency.ts`. It checks distinct backend sessions, duplicate/cumulative payments, competing excessive refunds, advisory-lock contention/release, and concurrent immutable wallet settlement. The harness refuses any target except a local database named `jc_ledger_test`; that refusal was checked locally. It uses the already-locked `pg` driver provided by the existing `connect-pg-simple` dependency and does not connect to Replit or production.

Canonical reward funding now uses verified ledger totals instead of invoice gift fields when `JOB_PAYMENT_LEDGER_ENABLED=true`. It additionally requires `JOB_PAYMENT_REWARDS_ENABLED=true`, which remains disabled by default. Recipient settlement rechecks completed/paid status, approved USD quote, coverage, refund absence and persisted reward basis while holding the job row lock used by payment/refund writers. Disposable tests passed the disabled flag, $40 gift exclusion on a $100 job, rejection of mismatched saved funding, incomplete-job and refund holds, and a 600-token wallet credit at the saved rate. The server build passed. Automatic triggering/outbox, real provider acceptance, refund policy, and coordination with every quote/completion writer remain open; this is not approval to enable either flag.

The PostgreSQL concurrency harness also exercises this canonical reward path: concurrent attempts credit one 600-token gift-adjusted award; a second scenario observes the wallet transaction blocked on a refund's job lock using `pg_blocking_pids`, commits the refund, and requires zero reward/wallet rows afterward. These cases and full CI passed at f87aa2e5, run 34386970989. Later queue/worker changes require their own CI.

September 9 refund follow-up: the disabled Square adapter now retrieves the refund and original payment, checks completed status/location/USD and stored job/quote association, and records both in one transaction. Refund-first delivery cannot briefly set a paid marker; an invalid refund rolls back the original payment too. Replay creates no duplicate rows. Funding follows the original card, regardless of refund destination. Tipped refunds require explicit allocation and are currently rejected; catch-up also requires the original capture timestamp. Focused refund verification and disposable database tests passed, as did the server build. This addresses local out-of-order recording; live provider routing, tipped-refund support, concurrent sessions, production schema/restore and reward policy remain open.

September 9 reward audit: the existing disbursement service used `pool.query` for a session advisory lock and its unlock. These may execute on different connections, invalidating the intended serialization. The lock now holds one checked-out connection through disbursement and unlock; uncertain connections are discarded. Focused connection ownership, contention, acquisition failure and unlock failure tests and the server build passed. Real competing PostgreSQL sessions remain an acceptance requirement.

Reward retries now settle the locked ledger's persisted amount, rate and quote values and validate its job/recipient identity. Payout summaries use persisted amounts too. An existing reward without the ledger's credited marker is rejected for reconciliation because its prior wallet effect is ambiguous. `scripts/check-job-ledger-settlement.ts` passed against disposable PGlite: a failed wallet update rolls back the reward and wallet creation, a changed-rate retry credits the original 1,000-token award, replay adds nothing, and identity/ambiguous-history failures leave the balance unchanged. The server build passed. This validates settlement transactions, not the complete job/reward workflow or concurrent sessions.

- The disabled Square card adapter now retrieves GetPayment by ID, requires COMPLETED/USD/configured location, resolves the order to exactly one stored job/quote pair, and separates sandbox/production provider identities. It rejects unknown funding types and refunded payments; Square gift-card principal is excluded from earning. Wire it only after signature-path, live-provider, refund and migration acceptance. Other rails remain unimplemented.
- Reconcile existing paid markers before migration, including jobs whose approved total increased after payment. Payments now record bounded metadata and an approved source quote revision belonging to the job in the same currency; settlement requires the current approved quote and lead total to agree. Existing ledger installations need quote-revision backfill before adding the non-null revision column.
- Completed refund recording is now implemented with unique provider/refund identity, payment association, cumulative over-refund/funding checks, and net payment/gift reconciliation. The report lists refund IDs and flags affected paid/reward markers for review without changing balances or requesting a provider refund. Disposable PostgreSQL tests pass duplicate/conflict, excessive refund, wrong-funding and unknown-payment cases. Provider refund verification/routing, out-of-order refund arrival, and owner-approved treatment of already-issued rewards remain open; no automatic reward reversal policy is approved.
- Add a durable reward-trigger queue behind a separate disabled-by-default flag, using the existing completed-and-paid gates. Preserve gift-funded exclusions and editable reward rates.
- The read-only admin/owner endpoint `/api/admin/payments/reconciliation/:leadId` returns one consistent database snapshot of payment IDs, totals, gift funding, quote and reward markers, and mismatch reasons. It returns disabled without querying the ledger while the feature flag is off. An expandable admin-only job-detail panel presents the report with refresh/loading/error, disabled and empty states. A synthetic desktop browser preview verified report rendering. Local HTTP tests with simulated identities passed anonymous 401, employee/customer 403, admin/business-owner 200, invalid ID 400, missing job 404, sanitized failure 500, and private/no-store headers. Live account/owner-page and mobile-device acceptance, refund information and failed reward attempts remain open.
- Verify competing payments in separate PostgreSQL sessions, transaction failures and replay, actual provider signatures/server verification, refund ordering, both payment/completion orders, reward exactly-once behavior, and wallet/ledger balances.
- Complete an isolated production-schema/restore check and one specifically authorized live payment test before switching any adapter or enabling automatic rewards.

Run the disposable database check with an installed PGlite module:

```text
node --import tsx scripts/check-job-payment-ledger.ts <path-to-pglite-dist-index.js>
```

The harness replaces the connection method only within its own process and uses synthetic jobs. It does not read production data or apply migrations to the configured database.

The adapter orchestration test verifies retrieval → stored association → settlement ordering and confirms that provider failures, missing/ambiguous associations, invalid payment snapshots, and ledger failures are not acknowledged as successful. The disposable PostgreSQL harness also runs this orchestration into the actual ledger transaction with a simulated Square gift-card payment; the job settles with zero customer-eligible gift-funded cents and no rewards. These use a simulated provider, not a live Square credential or webhook.

The same harness verifies a reconciliation mismatch after a paid job's approved total increases from $2,000 to $2,500: it reports the $500 shortfall and flags paid/reward markers without modifying either. This surfaces the inconsistency; automatic correction and reward reversal are not implemented.

Square adapter checks follow the [Payment object](https://developer.squareup.com/reference/square/objects/payment) and [refund documentation](https://developer.squareup.com/docs/payments-api/refund-payments). Principal uses amountMoney, excluding tips; COMPLETED alone is insufficient because refunded payments retain that status. Cross-method gift-card refunds require separate refund-event handling before activation. No provider API was called during local mapping tests.
